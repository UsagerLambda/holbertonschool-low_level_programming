#include "main.h"
/**
 * _isdigit  - Entry point
 * @c: variable int
 * Description : if digit return 1 if not return 0
 * Return: Alway 0 (Success)
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
return (1);
else
return (0);
return (0);
}
