#include "main.h"
/**
 * mul  - Entry point
 * @a: variable int a
 * @b: variable int b
 * Description : multiply a and b
 * Return: Alway 0 (Success)
 */
int mul(int a, int b)
{
int c = a * b;
return(c);
return (0);
}
