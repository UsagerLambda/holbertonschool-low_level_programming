#include <stdio.h>
/**
 * main - Entry point
 * Description: print all single digit number with no char
 * Return: Always 0 (Success)
 */
int main(void)
{
int n;
for (n = '1'; n <= '9'; n++)
{
putchar(n);
}
{
putchar('\n');
}
return (0);
}
