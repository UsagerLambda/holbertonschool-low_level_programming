#include <stdio.h>
/**
 * main - Entry point
 * Description: print all single digit number in base 10
 * Return: Always 0 (Success)
 */
int main(void)
{
char digit;
for (digit = 0; digit <= 9; digit++)
{
printf("%d",digit);
}
{
printf("\n");
}
return (0);
}
