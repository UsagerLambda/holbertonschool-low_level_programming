#include <stdio.h>
/**
 * main - Entry point
 * Description: print the lowercase alphabet in reverse
 * Return: Always 0 (Success)
 */
int main(void)
{
char  za;
for (char za = 'z'; za >= 'a'; za--)
{
putchar(za);
}
{
putchar('\n');
}
return (0);
}
