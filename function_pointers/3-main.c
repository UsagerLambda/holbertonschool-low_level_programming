#include <stdlib.h>
#include "3-calc.h"
/**
 * array_iterator 
 * @array: 
 * @size: 
 * @action: 
 * Return: 
 */
int main(argc, *argv[])
{

if (argc != 4)
{
	printf("Error\n");
	exit(98);
}
num1 = atoi(argv[1]);
operator = argv[2];
num2 = atoi(argv[3]);

if (argv[i][0] != '+' && argv[i][0] != '-' && argv[i][0] != '*' && argv[i][0] != '/' && argv[i][0] != '%')
}
