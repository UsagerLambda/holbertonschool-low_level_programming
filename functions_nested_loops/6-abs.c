#include "main.h"
/**
 * _abs  - Entry point
 * @int: integer
 * Description : printf alphabet in lower case using only putchar
 * Return: Alway 0 (Success)
 */
int _abs(int)
{
int n = 1;
if (n < 0)
_putchar(n);
else
_putchar(n);
return (0);
}
