#include "main.h"
/**
 * reset_to_98 - Entry point
 * @n: taille du triangle
 * Description : print hashtag in triangle
 * Return: Alway 0 (Success)
 */
void swap_int(int *a, int *b)
{
int temp = 0;
temp = *a;
*a = *b;
*b = temp;
}
